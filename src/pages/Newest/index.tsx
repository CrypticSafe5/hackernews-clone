import { getItem,  } from '../../requests/hackernews';
import React, { useEffect } from 'react';

export default function Newest() {
	useEffect(() => {

	}, []);

	return (
		<>Newest</>
	);
}
